# Descriptive statistics, how many in each group, etc.


---
title: "07_analysis3"
author: "s231932"
format: html
editor: visual
---

## Stacked bar plot for age distribution by gender

```{r}

bar_plot_age_gender <- dat_aug_03 |>
  ggplot(aes(x = Age, 
             fill = Sex)) +
  geom_bar(position = "stack", 
           stat = "count", 
           alpha = 0.7) +
  labs(title = "Overview of age and gender repartition for patients with cyrrhosis",
       x = "Age Group",
       y = "Count") +
  scale_color_viridis(discrete = TRUE, option = "D") +
  theme_minimal() 
  
```

```{r}
bar_plot_age_gender
```



# Overview of the disease they had 

```{r}
bar_plot_age_gender2 <- dat_aug_03 |> 
  ggplot(aes(x = Liver_disease, 
             fill = Mortality)) +
  geom_bar(position = "stack", 
           stat = "count", 
           alpha = 0.7) +
  labs(title = "Overview of cause of cyrrhosis repartition startified by mortality",
       y = "Count") +
  scale_fill_viridis(discrete = TRUE, option = "D") +
  theme_minimal() +
  theme(axis.title.x=element_blank()) +
  scale_x_discrete(guide = guide_axis(n.dodge=4))

```


```{r}
bar_plot_age_gender2
```

# Overview of comorbities

```{r}
bar_plot_age_gender3 <- dat_aug_03 |> 
  ggplot(aes(x = Comorbidities, 
             fill = Comorbidities)) +
  geom_bar(alpha = 0.7) +
  labs(title = "Overview of comorbidities across patieents suffering from cyrrhosis ",
       y = "Count") +
  scale_fill_viridis(discrete = TRUE, option = "D") +
  theme_minimal() +
  theme(axis.text.x=element_blank(), 
        legend.title=element_blank()) 

```

```{r}
bar_plot_age_gender3
```

